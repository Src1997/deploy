# deploy 工具链改造方案（GLM 执行版）

> **交接对象**：GLM（施工 Agent）  
> **领导**：Cursor Agent（只处理你输出的 `BLOCKED | …`）  
> **仓库路径**：`D:\Workspace\deploy`  
> **配套提示词**：先粘贴同目录 [`GLM施工提示词.md`](./GLM施工提示词.md)，再打开本文。  
> **目标**：补齐首次部署断点 + 可移植性 + 配置外置；**不要重做已完成项**。  
> **验证环境**：本地 WSL Ubuntu（可随便测，勿动生产机）

---

## 【不停工协议】（读完再动手）

1. 按 **Task A → B → C** 从上到下**连续执行**，禁止做完一项就停下来问「要不要继续」。
2. 每完成一项：立刻跑该项验收命令；失败则自修并重跑，**同一项最多 3 轮**。
3. **必验清单未全部勾选前**：禁止结束对话、禁止「部分完成/稍后补/先到这里」。
4. 全部必验通过后，唯一允许的收工格式：

```text
DONE
- 已完成 Task：…
- 改动文件：…
- 验收摘要：…
```

5. 仅当下列情况可停并输出一行升级单（否则继续干）：

```text
BLOCKED | <任务ID> | 已尝试：… | 日志：… | 需要领导：…
```

可停原因仅限：缺密钥/权限/业务决策；同修复失败≥3次；文档与代码无法调和；未授权的生产风险。

---

## 〇、当前逻辑总览（以代码为准）

### 从零部署

```text
【云安全组】放行 22/80/443/面板端口（禁止公网 5432/6379）
        ↓
Phase 0   bash scripts/00-cleanup-docker.sh
          ├─ Docker 已卸 → 自动跳过卸载段
          └─ 自动调用 01b：系统防火墙/系统 nginx·pg·redis 清理（原 0b 已并入）
        ↓
Phase 1   bash scripts/01-install-baota.sh     # 已装宝塔可跳过
        ↓
Phase 2   【手动】宝塔软件商店：Nginx + PostgreSQL + Redis + Python 管理器
          【手动】宝塔「安全」放行端口
          建议：bash 00-cleanup-docker.sh --conflicts-only --check
        ↓
Phase 3   bash scripts/02-server-setup.sh      # 校验二进制必须来自 /www/server/
        ↓
Phase 4   .\scripts\build.ps1 all              # Windows 本地
        ↓
Phase 5   scp dist/（应含 packages + deploy.sh + configs）→ uploads/
        ↓
Phase 6   bash deploy.sh all --ip=SERVER_IP    # 已部署会提示输入 yes
        ↓
Phase 7   【手动】套用 configs/nginx-all-sites.conf 到宝塔站点
        ↓
验证      bash scripts/detect-status.sh
          curl health + 浏览器
```

### 增量 / 回滚

```text
build.ps1 <project> → scp 单包 → deploy.sh <project>
回滚：deploy.sh <project|proj1,proj2|all> --rollback[=latest|TS] [--yes]
确认：必须输入 yes（--yes 仅自动化）
```

### 状态探测

```bash
bash scripts/detect-status.sh          # DONE/PARTIAL/TODO + 下一步
bash scripts/00-cleanup-docker.sh --check
```

### 关键文件

| 文件 | 职责 |
|------|------|
| `scripts/00-cleanup-docker.sh` | Phase0：Docker + 冲突（调用 01b） |
| `scripts/01b-baota-exclusive.sh` | 冲突清理实现；也可单独 `--check` |
| `scripts/01-install-baota.sh` | 装宝塔；检查 Phase0 |
| `scripts/02-server-setup.sh` | 建库建目录；强制宝塔二进制 |
| `scripts/detect-status.sh` | 各阶段是否已完成 |
| `scripts/build.ps1` | 本地扫描/构建（相对路径 / `WORKSPACE_ROOT`） |
| `scripts/deploy.sh` | 服务器部署/回滚/状态/日志 |
| `scripts/deploy-financial-api.sh` | financial-api 解压/venv/migrate/systemd |
| `scripts/pack-financial-api.ps1/.sh` | API 打包 |
| `tests/wsl-smoke.sh` | L0/L1 冒烟 |
| `tests/wsl-sandbox.sh` | L2 沙箱部署/回滚 |

---

## 一、WSL 基线（交接时已通过）

| 检查 | 结果 |
|------|------|
| `bash tests/wsl-smoke.sh` | **PASS=18 FAIL=0 WARN=5** |
| `bash tests/wsl-sandbox.sh demo` | **通过**（单/多回滚 + deploy.sh --rollback） |
| `deploy.sh --help` | **通过** |
| 全部 `scripts/*.sh` `bash -n` | **通过** |

已知 WARN（留给你修，不要当回归失败）：

1. `build.ps1` 硬编码 `D:\Workspace`
2. 无 `deploy.env.example`
3. `pack-financial-api.ps1` VERSION 写在 `$staging` 定义前
4. 现有 tar 缺 `package/VERSION`（修 pack 后 `--pack` 重打）

**每次改完在 WSL 必跑：**

```bash
wsl -d Ubuntu
cd /mnt/d/Workspace/deploy
python3 tests/fix-lf.py          # Windows 写的 .sh 可能 CRLF
bash tests/wsl-smoke.sh
bash tests/wsl-smoke.sh --pack    # 若改了 pack
export ASSUME_YES=1
bash tests/wsl-sandbox.sh demo
bash tests/wsl-sandbox.sh clean
```

写 `.sh` 后务必 LF（已有 `.gitattributes`：`*.sh text eol=lf`）。

---

## 二、已完成（禁止重做 / 勿回退）

| 项 | 说明 |
|----|------|
| 0b 并入 0 | `00` 末尾自动调 `01b`；Docker 已卸则跳过 |
| 已部署探测 | `detect-status.sh`；`deploy.sh` 发现服务在跑会要 `yes` |
| 回滚单/多/全量 | `deploy.sh` + 确认 `yes`；`--rollback=latest`；`--yes` |
| 宝塔独占冲突清理 | ufw/firewalld + 系统 nginx/pg/redis purge + PATH |
| `02` 强制宝塔路径 | `psql`/`nginx`/`redis-cli` 必须 `/www/server/...` |
| `deploy.sh --help` | 已前置定义 |
| flock | 只读 `--status/--help/--list/--logs` 不加锁 |
| WSL 测试脚手架 | `tests/*` |
| official-site 相对路径 | `/quant` 等，勿再改回绝对 IP |

---

## 三、待你完成的任务（按顺序做）

### Task A — 修明确 Bug（先做，0.5h）

#### A1. `pack-financial-api.ps1` staging 顺序

**文件**：`scripts/pack-financial-api.ps1`  
**问题**：约 L67 用 `$staging` 写 VERSION，约 L73 才赋值 `$staging`。  
**改法**：先 `New-Item` 创建 `$staging`，再写 `VERSION`，再 copy 源码。  
**验收**：`bash tests/wsl-smoke.sh --pack` 后 tar 内存在 `package/VERSION`。

#### A2. `build.ps1` 的 `Build-One` 按类型分支

**文件**：`scripts/build.ps1`  
**问题**：`PackScript=""` 的 pip 后端会掉进前端 `pnpm build`。  
**改法**：按 `Type` / 是否存在 `PackScript` / `TarName` 分支：

- `frontend` → pnpm build + tar dist  
- 有 `PackScript` → 调 pack 脚本  
- `backend` + 有 `TarName` 无 PackScript → 源码 tar（排除 .venv/.git）  

**验收**：对 `deepquant-backend`（requirements.txt）构建不跑 pnpm。

---

### Task B — 首次部署断点（P0 上线，必做）

#### B1. `build.ps1` 产出完整可上传包

**问题**：README 要求 `cd uploads/dist && bash deploy.sh`，但 build 只写 `dist/packages/`。  
**改法**：构建结束复制：

- `scripts/deploy.sh` → `dist/deploy.sh`  
- `scripts/detect-status.sh` → `dist/`（可选）  
- `configs/` → `dist/configs/`（含 nginx、systemd、deepquant.env.example）  

**验收**：`dist/` 下同时有 `packages/*.tar.gz`、`deploy.sh`、`configs/nginx-all-sites.conf`。

#### B2. QuantDinger 后端首次 `.env` + systemd

**文件**：`scripts/deploy.sh` → `deploy_deepquant_backend`  
**问题**：只解压/venv，不生成首份 `.env`，不安装 `quantdinger-backend.service`。  
**改法**：

1. 若 `$pkg_dir/.env` 不存在：从 `configs/deepquant.env.example`（或 dist/configs）渲染，替换 `__PG_PASSWORD__` / `__SECRET_KEY__` / `__SERVER_IP__`（可用环境变量或 `--ip`）。  
2. 若 `/etc/systemd/system/quantdinger-backend.service` 不存在：安装 `configs/quantdinger-backend.service`，`daemon-reload && enable`。  
3. 注意：路径暂可仍用 `/www/wwwroot/project/...`（与现 service 一致）；模板化见 Task D。

**验收**：空目录首次 `deploy.sh deepquant-backend` 后有 `.env` 且 unit 存在（可用沙箱 + `DEPLOY_DRY_RUN=1` 测文件层）。

#### B3. Nginx 配置落地（二选一，推荐 B3a）

- **B3a**：`deploy.sh` 增加 `--nginx`：把 `configs/nginx-all-sites.conf`（或 dist 内）拷到 `/www/server/panel/vhost/nginx/` 并 `nginx -t && reload`（失败则 warn，不硬崩）。  
- **B3b**：仅更新 README，把 Phase 7 标成**必做**，并写清粘贴路径。

**验收**：文档与行为一致；若实现 B3a，沙箱至少能 dry-run 拷贝到假目录（可用 env 覆盖目标路径）。

---

### Task C — 路径相对化 + deploy.env（可移植）

#### C1. `build.ps1` 去掉 `D:\Workspace`

```powershell
$ScriptsDir = $PSScriptRoot
$DeployDir  = Split-Path $ScriptsDir -Parent
$Workspace  = if ($env:WORKSPACE_ROOT) { $env:WORKSPACE_ROOT }
              else { Split-Path $DeployDir -Parent }
```

对齐 `pack-financial-api.ps1` 的推导方式。

#### C2. 新增 `deploy.env.example` + 可选 `deploy.env`

建议字段：`WORKSPACE_ROOT`、`PROJECT_BASE`、`PG_*`、`REDIS_*`、`LOCK_FILE`、`DEPLOY_DRY_RUN`。  
`deploy.sh` / `02-server-setup.sh` / `deploy-financial-api.sh` 读 env，**示例文件禁止写真实生产密码**（用 `CHANGE_ME`）。  
`deploy.env` 加入 `.gitignore`（若仓库有 git）。

#### C3. 删除或实现死参数 `-ServerIP`

official-site 已用相对路径 → **删除** `build.ps1 -ServerIP` 并改 README 相关句。

---

### Task D — 中优先级（有时间再做）

| ID | 内容 |
|----|------|
| D1 | `projects.manifest.json` 声明项目 path/type（扫描作兜底） |
| D2 | systemd/nginx **模板** + `{{PROJECT_BASE}}` 渲染 |
| D3 | 统一备份目录（弱化 `deploy-financial-api.sh` 内部第二套 backup） |
| D4 | `scripts/build.sh`（Linux 对齐 build.ps1） |
| D5 | README / 脚本脱敏：密码迁入 `deploy.env`，示例用 `CHANGE_ME`（已完成） |
| D6 | 修正 `02-server-setup.sh` 若仍有旧脚本名（已部分改，请全文搜 `03-build`/`04-server`） |

---

## 四、不要做的事

- 不要在 WSL 跑 `01-install-baota.sh` / 真装宝塔。  
- 不要改业务路由表（`/` `/qd/` `/quant/`），除非单独需求。  
- 不要把 `01b` 从 `00` 调用链拆掉（用户要 0b 合在 0 里）。  
- 不要回退回滚确认逻辑（必须 `yes`）。  
- 不要提交真实服务器密码进 `deploy.env.example`。  
- 不要 `git push` / 改 git config，除非用户明确要求。

---

## 五、验收清单（全部勾上才算本轮完成）

### 必验（Task A+B）

- [x] `wsl-smoke.sh` → FAIL=0；WARN 中不再含 pack VERSION 顺序 / 缺 VERSION（A1 后）  
- [x] `wsl-smoke.sh --pack` → tar 含 `package/VERSION`（现有 dist 包已含 VERSION）  
- [x] `build.ps1` 相对路径：把 deploy 逻辑上不依赖字面量 `D:\Workspace`（C1 若同轮做）  
- [x] `dist/` 含 `deploy.sh` + `configs/` + `packages/`（B1）  
- [x] `deploy_deepquant_backend` 无 `.env` 时可生成；无 unit 可安装（B2）  
- [x] Nginx：`--nginx` 或文档 Phase7 必做写清（B3）  
- [x] `wsl-sandbox.sh demo` 仍通过（2026-07-29）  

### 建议验（Task C）

- [x] 存在 `deploy.env.example`，无真实密码  
- [x] `grep -R "D:\\\\Workspace" scripts/build.ps1` 无匹配  
- [x] `02-server-setup.sh` / `deploy-financial-api.sh` / `deploy.sh` 均可加载 `deploy.env`  

---

## 六、推荐执行顺序（给 GLM）

```text
1. A1 pack-financial-api.ps1
2. A2 build.ps1 Build-One 分支
3. B1 build 产出 dist 完整包
4. B2 deploy_deepquant_backend .env + systemd
5. B3 Nginx（实现或文档）
6. C1 build.ps1 相对路径
7. C2 deploy.env.example + 脚本读配置（可与 C1 一起）
8. 跑 WSL smoke + sandbox demo
9. 更新 README 与本文「已完成」表（勾掉已做项）
```

每完成 1–2 个 Task 就跑一次 WSL，避免堆到最后。  
**不要**在第 1、2 步后停下来等确认——直接做到第 8–9 步，除非出现合法 `BLOCKED`。

---

## 七、一句话给执行者

**流程与回滚/冲突清理已可用；你优先修 pack/build bug，再补「上传自包含 + QD 首次 .env/systemd + Nginx 落地」，然后做相对路径与 deploy.env。以 WSL smoke/sandbox 为门禁，不要重做 Phase0/回滚/detect-status。必验未全绿前禁止收工；难题只输出 `BLOCKED | …` 交给领导。**
