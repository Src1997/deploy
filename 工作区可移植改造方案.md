# deploy 工作区可移植改造方案（GLM 执行版）

> **交接对象**：GLM（施工 Agent）  
> **领导**：Cursor Agent（只处理 `BLOCKED | …`）  
> **仓库**：`D:\Workspace\deploy`  
> **目标**：本地构建/打包与服务器部署改为**配置驱动**，换工作区根目录、换相对路径不必改脚本硬编码。  
> **不做**：通用 CI/CD、任意语言运行时发现、重写宝塔/Nginx 拓扑为完全动态（本期只做项目清单 + 路径映射）。  
> **验证**：本地 PowerShell + WSL；**勿动生产机**除非文档明确授权。

---

## 【不停工协议】

1. 按任务清单从上到下连续执行，不要一次只做一项就停下来汇报。
2. 每完成一项：立刻跑文档里的验收命令；通过则勾选，失败则自行修复并重跑，最多自动重试 3 轮。
3. 未遇到 BLOCKED 条件前：禁止结束对话、禁止问「是否继续 / 要不要我做下一个」。
4. 全部必验项勾选且验收命令通过后，才允许输出「DONE」总结（含改了哪些文件 + 验收输出摘要）。
5. 只有下列情况才能停下来 @领导（本 Cursor Agent）：
   - 缺密钥/服务器权限/用户未提供的业务决策；
   - 同一处修复失败 ≥3 次且原因不明；
   - 需求与文档明显矛盾且无法从仓库推断；
   - 可能破坏生产且文档未授权。
   停下来时必须写：BLOCKED | 任务ID | 已尝试 | 日志摘要 | 需要领导做什么。

---

## 1. 目标与禁止重做

### 目标

1. **工作区根可配**：`WORKSPACE_ROOT`（或 `projects.yaml` 内 `workspaceRoot`）决定扫描/打包源码根。
2. **项目清单可配**：哪些 id 可 build/deploy、源码相对路径、产物名、服务器目标路径、systemd 单元、健康检查 —— 全部来自清单文件，**禁止**在 `pack-*.ps1` / `deploy.sh` 写死 `financial\financial-api` 一类路径作为唯一真相。
3. **兼容现状**：默认清单描述当前 `D:\Workspace` + `/www/wwwroot/project` 拓扑；现有命令 `.\scripts\build.ps1 financial-web` / `bash deploy.sh financial-api` 行为不变。
4. **可选自动发现**：扫描仅作「建议补全清单」或校验，**不以扫描结果覆盖清单中的 path**（避免误伤 monorepo）。

### 禁止重做

- 不要重做密码外置 / `load-deploy-env.sh` / `deploy.env` 已完成项。
- 不要改生产 Nginx 业务路由语义（仍用现有 conf 模板）。
- 不要引入 Python/Node 构建 deploy 工具链（保持 bash + PowerShell）。
- 不要为「任意 Git 仓库自动 systemd」做过度抽象。

---

## 2. SSOT：`projects.yaml`

**新建** [`projects.yaml`](./projects.yaml)（与 `deploy.env` 同级；可提交 Git）。

```yaml
# deploy/projects.yaml — 项目清单（SSOT）
version: 1

# 本地源码根：空则 = deploy 父目录；也可被环境变量 WORKSPACE_ROOT 覆盖（env 优先）
workspaceRoot: ""

# 服务器项目根：可被 deploy.env 的 PROJECT_BASE 覆盖（env 优先）
projectBase: /www/wwwroot/project

projects:
  - id: financial-web
    enabled: true
    kind: frontend          # frontend | backend
    displayName: 行情/社区前端
    # 相对 workspaceRoot
    sourcePath: financial/financial-web
    build:
      packageManager: pnpm
      script: build           # 或 build:prod（若存在则优先，逻辑与现 build.ps1 一致）
      distDir: dist
      artifact: financial-web-dist.tar.gz
    # 相对 projectBase
    deployPath: financial/financial-web/dist
    services: []
    healthUrl: ""
    nginxReload: true

  - id: financial-api
    enabled: true
    kind: backend
    displayName: FastAPI 后端
    sourcePath: financial/financial-api
    build:
      packer: scripts/pack-generic.ps1   # 见 Task B；替代写死路径的 pack-financial-api.ps1
      # packer 入参由 id + sourcePath 注入，不再写死 financial\financial-api
      artifactPattern: financial-api-*.tar.gz
      # 打包时附带的服务器部署脚本与 unit（相对 deploy 根）
      include:
        - scripts/deploy-financial-api.sh
        - configs/systemd/financial-api.service
        - configs/systemd/financial-crawler.service
        - configs/systemd/financial-worker.service
        - configs/systemd/financial-streaming.service
        - configs/financial-api.env.example
    deployPath: financial/financial-api/package
    services:
      - financial-api
      - financial-crawler
      - financial-worker
      - financial-streaming
    healthUrl: http://127.0.0.1:5001/api/health
    nginxReload: false
    # 可选：专用部署钩子（相对 deploy 根）；空则走通用解压逻辑
    deployHook: scripts/deploy-financial-api.sh

  - id: official-site
    enabled: true
    kind: frontend
    displayName: 卓筹介绍站
    sourcePath: official-site          # 按仓库实际路径改；若嵌套则写全相对路径
    build:
      packageManager: pnpm
      script: build
      distDir: dist
      artifact: official-site-dist.tar.gz
      envFile: configs/official-site.env
    deployPath: official-site/dist
    services: []
    healthUrl: ""
    nginxReload: true

  - id: deepquant-web
    enabled: true
    kind: frontend
    displayName: QuantDinger 前端
    sourcePath: deepquant/deepquant_vue  # 与现 NameMapping 对齐；以仓库实路径为准
    build:
      packageManager: pnpm
      script: build
      distDir: dist
      artifact: deepquant-web-dist.tar.gz
    deployPath: deepquant/web/dist
    services: []
    healthUrl: ""
    nginxReload: true

  - id: deepquant-backend
    enabled: true
    kind: backend
    displayName: QuantDinger 后端
    sourcePath: deepquant/deepquant/backend_api_python  # 以仓库实路径为准
    build:
      mode: source-tar                   # pip/requirements 式整包
      artifact: deepquant-backend-package.tar.gz
      exclude:
        - .env
        - .venv
        - venv
        - logs
        - __pycache__
        - .git
    deployPath: deepquant/backend/package
    services:
      - quantdinger-backend
    healthUrl: http://127.0.0.1:5000/health
    nginxReload: false
```

### 解析优先级

```text
WORKSPACE_ROOT env  >  projects.yaml workspaceRoot  >  deploy 父目录
PROJECT_BASE env    >  projects.yaml projectBase    >  /www/wwwroot/project
```

### 辅助：名称别名（可选）

若需保留旧目录名展示，在 yaml 增加：

```yaml
aliases:
  deepquant_vue: deepquant-web
  backend_api_python: deepquant-backend
```

`build.ps1` 扫描到别名目录时映射到 `id`，**path 仍以 yaml `sourcePath` 为准**。

---

## 3. 有序 Task

### Task A — 清单文件 + 加载器

**改：**

- 新建 `projects.yaml`（按上文填满现有 5 个项目；`sourcePath` 以本机 `D:\Workspace` 实路径核对）
- 新建 `scripts/lib/load-projects.ps1`：读 yaml → PSCustomObject 列表  
  - Windows：优先 `powershell-yaml` 若已装；**否则**用轻量解析（仅支持本文件子集）或附带 `projects.json` 双写  
  - **选定实现**：同时提交 `projects.json`（由 yaml 手工同步或脚本生成），PowerShell/`deploy.sh` **读 JSON**，避免强依赖外部模块。维护时以 `projects.yaml` 为人类编辑源，`projects.json` 为机器源；Task A 验收要求两者字段一致。
- 新建 `scripts/lib/load-projects.sh`：`jq` 读 `projects.json`（若无 jq：用 python3 -c json）
- `deploy.env.example` 补充注释：`WORKSPACE_ROOT=`、说明清单文件路径 `PROJECTS_FILE=`（默认 `$DeployDir/projects.json`）

**验收：**

```powershell
cd D:\Workspace\deploy
# 加载后打印 5 个 id 与 sourcePath（写一小段 scripts/lib/_probe-projects.ps1 或一次性命令）
powershell -NoProfile -File scripts\lib\_probe-projects.ps1
# 期望 stdout 含 financial-web, financial-api, official-site, deepquant-web, deepquant-backend
```

```bash
# WSL
cd /mnt/d/Workspace/deploy && bash scripts/lib/_probe-projects.sh
```

- [ ] Task A 验收通过

---

### Task B — 本地 build/pack 读清单

**改：**

- `scripts/build.ps1`  
  - `$Workspace` 按优先级解析  
  - **主列表来自** `projects.json`（`enabled:true`），不再把「扫描到的任意目录」当作可部署全集  
  - 扫描仅用于：报告「工作区存在但未登记」警告；或 `--discover` 打印建议 yaml 片段（可选）  
  - `Build-One`：用条目的 `sourcePath` 拼绝对路径；前端 `distDir`/`artifact`/`envFile` 来自清单  
  - 后端：调用通用 packer，传入 `-ProjectId -SourceDir -ArtifactDir -Include ...`
- 新建 `scripts/pack-generic.ps1`（参数化打包）：  
  - 参数：`SourceDir`, `OutDir`, `NamePrefix`（如 financial-api）, `IncludeFiles[]`  
  - **删除对** `Join-Path $Workspace 'financial\financial-api'` **的依赖**
- `scripts/pack-financial-api.ps1`：改为薄封装，读清单中 `financial-api` 再调 `pack-generic.ps1`（保留旧命令入口）
- `scripts/pack-financial-api.sh`：同样改为读 JSON + 参数化路径

**验收：**

```powershell
cd D:\Workspace\deploy
$env:WORKSPACE_ROOT = "D:\Workspace"
.\scripts\build.ps1 financial-web
# 期望：packages 下生成 financial-web-dist.tar.gz

# 临时改 projects.json 里 financial-api.sourcePath 为错误路径再 build → 应明确报错含该路径
# 改回后：
.\scripts\build.ps1 financial-api
# 期望：packages 下有 financial-api-*.tar.gz，且包内无错误源
```

- [ ] Task B 验收通过

---

### Task C — 服务器 deploy.sh 读清单

**改：**

- `scripts/deploy.sh`（及复制到 dist 的同名文件生成链路）：  
  - 加载 `projects.json`（从 `$DIST_ROOT/projects.json` 或 `$SCRIPT_DIR/../projects.json`；`build.ps1` 的 `Copy-DeployAssets` **必须把 projects.json 拷进 dist/**）  
  - 删除硬编码 `declare -a PROJECTS=(...)` 与手写 `FINANCIAL_*_DIR` 作为唯一来源；改为循环清单生成关联数组：  
    - `DEPLOY_PATH[id]=$PROJECT_BASE/$deployPath`  
    - `ARTIFACT[id]=...`  
    - `SERVICES[id]=...`  
    - `HEALTH[id]=...`  
  - `deploy_*` 函数：优先通用 `deploy_by_id "$id"`；`deployHook` 非空则调用钩子并传入解压目录  
  - 保持 CLI：`bash deploy.sh financial-web,financial-api` / `all` / `--rollback` 兼容

**验收（WSL 干跑）：**

```bash
cd /mnt/d/Workspace/deploy
export DEPLOY_DRY_RUN=1
export PROJECT_BASE=/tmp/deploy-sandbox/project
# 准备假 packages 或跳过解压的 dry-run 分支
bash scripts/deploy.sh --help | head
bash scripts/lib/_probe-projects.sh
# 若已有 tests/wsl-smoke.sh：扩展断言「PROJECTS 来自 json 长度=enabled 数」
```

- [ ] Task C 验收通过

---

### Task D — 文档与示例

**改：**

- `README.md` 增加一节「项目清单 / 换工作区」：  
  1. 复制/编辑 `projects.yaml` → 同步 `projects.json`（或运行 `scripts/sync-projects.ps1`）  
  2. 设置 `WORKSPACE_ROOT`  
  3. 服务器只需改 `PROJECT_BASE` + 清单中 `deployPath`  
- `deploy.env.example` 注释与示例对齐  
- 本文件总验收勾选

可选：`scripts/sync-projects.ps1` 用简易转换；若不做转换脚本，则在 README 写明「改 yaml 后必须手改 json」——**推荐做 sync 脚本**（Node 或 Python3 均可，仓库已有 python）。

**验收：**

```powershell
# 若实现 sync：
.\scripts\sync-projects.ps1
# json 与 yaml 的 projects.id 集合一致
Select-String -Path README.md -Pattern "projects.yaml|WORKSPACE_ROOT" | Select-Object -First 5
```

- [ ] Task D 验收通过

---

## 4. 总验收清单（全勾才算完）

- [ ] 存在 `projects.yaml` + `projects.json`，五项目 path 与现网一致  
- [ ] `build.ps1` / `pack-*` **不再**写死 `financial\financial-api` 为唯一源码路径  
- [ ] `WORKSPACE_ROOT` 指向合法根时可构建；指向空目录时报错清晰  
- [ ] `deploy.sh` 项目列表与路径来自 `projects.json`  
- [ ] `dist/` 构建产物含 `projects.json`  
- [ ] 旧命令 `build.ps1 financial-web`、`deploy.sh financial-api --help` 仍可用  
- [ ] README 已说明如何登记新项目（加清单条目，而不是改 3 处硬编码）  
- [ ] 未引入对生产机的未授权变更  

---

## 5. BLOCKED 升级格式

```text
BLOCKED | <任务ID> | 已尝试：… | 日志：… | 需要领导：…
```

---

## 6. 范围外（明确不做 / 可二期）

| 项 | 说明 |
|----|------|
| 完全动态 Nginx server 块生成 | 仍用现成 conf；清单不驱动 location |
| 自动发现并部署任意 npm 包 | 必须登记到清单 |
| 多 workspace 并行 profile | 二期可用 `projects.dev.yaml` |
| 改写宝塔安装脚本 | 无关本主题 |

---

## 7. 登记新项目（改造完成后的用法）

1. 在工作区放好源码  
2. 编辑 `projects.yaml` 增加一条 `id` + `sourcePath` + `deployPath` + build/deploy 字段  
3. `.\scripts\sync-projects.ps1`  
4. 前端：确保有 `pnpm build`；后端：按需补 `deployHook` / systemd  
5. `.\scripts\build.ps1 <id>` → 上传 → `bash deploy.sh <id>`  
